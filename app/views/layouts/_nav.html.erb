<nav class="navbar navbar-inverse">
  <div class="container-fluid">
    <div class="navbar-header">
      <strong><a class="navbar-brand" href="<%= movies_path %>">Rotten Mangoes</a></strong>
    </div>
    <ul class="nav navbar-nav">
      <% if current_user %>
        <li class="dropdown">
          <a class="dropdown-toggle" data-toggle="dropdown" href="#"><%= current_user.fullname %>
          <span class="caret"></span></a>
          <%= render 'layouts/dropdown_options' %>
        </li>
      <% else %>
        <%= link_to "Login", new_session_path, class: "btn btn-warning navbar-btn" %>
        <%= link_to "Sign Up", new_user_path, class: "btn btn-warning navbar-btn" %>
      <% end %>
    </ul>
  </div>
</nav>